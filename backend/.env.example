# IntelliExtract Agno AI Backend Configuration

# Core Configuration
GOOGLE_API_KEY=your_google_gemini_api_key_here
LOG_LEVEL=INFO

# Model Configuration
DEFAULT_AI_MODEL=gemini-2.0-flash-001

# Monitoring Configuration (Optional)
# Get your Agno API key from: https://app.agno.com/settings
AGNO_API_KEY=your_agno_api_key_here
AGNO_MONITOR=false  # Set to 'true' to enable monitoring at app.agno.com/sessions
AGNO_DEBUG=true     # Set to 'true' to see AI reasoning & tool calls (without your JSON data)

# Development Configuration (Optional)
DEVELOPMENT_MODE=false  # Set to 'true' to enable development-specific features like caching

# Directory Configuration
# Set custom temp directory for agent operations (optional)
# If not set, system temp directory will be used
AGENT_TEMP_DIR=/tmp/intelliextract
# Alternative: Use relative path from project root
# AGENT_TEMP_DIR=./temp

# Environment Setup Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. For production monitoring, set AGNO_MONITOR=true and provide AGNO_API_KEY
# 3. For debugging issues, set AGNO_DEBUG=true temporarily
# 4. For development, set DEVELOPMENT_MODE=true to enable search result caching
# 5. Set AGENT_TEMP_DIR to control where Excel files are generated